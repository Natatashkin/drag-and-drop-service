import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => {
  return {
    container: {
      width: '80%',
      marginLeft: 'auto',
      marginRight: 'auto',
    },
  };
});
